struct и класс похожи, но обычно struct используется для хранения полей(как tuple),
а class - для более сложных объектов.

в class есть поля: private, public, protected
можно создать друзей написав friend "ранее объявленный класс/функция"

enum class Colors - класс-библиотека
{
    Red, Green, Blue
};

